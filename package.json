{
  "name": "Authentication",
  "version": "1.0.0",
  "description": "Passport local-authentication example using Express and MongoDB",
  "main": "server.js",
  "scripts": {
    "check": "node --check server.js && node --check routes.js && node --check api/auth/index.js && node --check api/auth/auth.controller.js && node --check middleware/security.js && node --check middleware/rateLimiter.js && node --check utils/validation.js && node --check utils/logger.js",
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "author": "Sumona Salma <sumona.uiu@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mongoose": "^9.9.4",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0"
  }
}
