{
  "name": "passportjs-auth-modernized",
  "version": "2.0.0",
  "private": true,
  "description": "Modernized Passport.js authentication application based on passportJs_auth",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config/env.js && node --check src/config/passport.js && node --check src/models/User.js && node --check src/routes/auth.routes.js && node --check src/routes/page.routes.js && node --check src/middleware/error.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "connect-mongo": "^5.1.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "mongoose": "^8.18.0",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {}
}
