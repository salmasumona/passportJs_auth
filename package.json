{
  "name": "passport-js-auth",
  "version": "2.0.0",
  "description": "Upgraded Passport.js authentication application with Express, MongoDB and server-side sessions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --check server.js && node --check config.js && node --check routes.js && node --check api/auth/auth.controller.js && node --check api/auth/auth.model.js && node --check api/auth/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "connect-mongo": "^5.1.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "mongoose": "^8.18.0",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "dotenv": "^17.2.1"
  },
  "private": true
}
